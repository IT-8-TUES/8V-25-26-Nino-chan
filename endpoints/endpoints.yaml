openapi: 3.0.3
info:
  title: Events API
  version: 0.1.0

tags:
  - name: HOME
  - name: LOGIN
  - name: REGISTER
  - name: ARCHIVE
  - name: LONG DESCRIPTION
  - name: SEARCH PAGE
  - name: PROFILE
  - name: VERIFY
  - name: RECOMMENDATIONS
  - name: PROFILE PICTURES

paths:

  /event/{id}:
    get:
      tags: [HOME, LONG DESCRIPTION]
      summary: HOME / LONG DESCRIPTION
      description: |
        normal

        Used by two pages:
        - HOME: path is a date (YYYY-MM-DD), returns events on that date as a list of {title, creator, id}.
        - LONG DESCRIPTION: path is an event ObjectId string, returns event detail
          {title, description, date, creator, creatorid}.
      parameters:
        - in: header
          name: jwt
          required: true
          schema:
            type: string
        - in: path
          name: id
          required: true
          description: date (YYYY-MM-DD) for HOME, event ObjectId string for LONG DESCRIPTION
          schema:
            type: string
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                oneOf:
                  - type: array
                    items:
                      $ref: '#/components/schemas/EventSummary'
                  - $ref: '#/components/schemas/EventDetail'

  /user/login:
    post:
      operationId: login
      tags: [LOGIN]
      summary: LOGIN
      description: Public. Authenticates a user and returns a JWT.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Credentials'
            example:
              email: user@example.com
              password: hunter2
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LoginResponse'
              example:
                code: 200
                jwt: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJ1c2VyaWQiOjQyfQ.signature

  /user/register:
    post:
      operationId: register
      tags: [REGISTER]
      summary: REGISTER
      description: Public. Creates a new user account. New accounts cannot publish events until verified by an admin.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RegisterBody'
            example:
              email: newuser@example.com
              password: hunter2
              username: newuser
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Code'
              example:
                code: 200

  /user/verify:
    post:
      operationId: verify
      tags: [VERIFY]
      summary: VERIFY
      description: |
        normal. Sends a verification request email to the admins so they can grant
        the user publishing rights. Requires the user to confirm their password.
      parameters:
        - in: header
          name: jwt
          required: true
          schema:
            type: string
          example: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJ1c2VyaWQiOjQyfQ.signature
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/VerifyRequest'
            example:
              password: hunter2
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Code'
              example:
                code: 200

  /user:
    patch:
      tags: [PROFILE]
      summary: PROFILE - update profile
      description: normal. User is identified by JWT — no userid needed in body.
      parameters:
        - in: header
          name: jwt
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ProfilePatch'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Code'

  /user/{userid}:
    get:
      tags: [ARCHIVE, PROFILE]
      summary: ARCHIVE / PROFILE
      description: |
        normal

        Used by two pages, distinguished by the `mode` query parameter:
        - ARCHIVE (?mode=archive): returns the user's bookmarked upcoming events as {title, creator, id}.
        - PROFILE (?mode=profile): returns the user's public profile {email, username, bio}.
      parameters:
        - in: header
          name: jwt
          required: true
          schema:
            type: string
        - in: path
          name: userid
          required: true
          schema:
            type: string
        - in: query
          name: mode
          required: true
          schema:
            type: string
            enum: [archive, profile]
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                oneOf:
                  - type: array
                    items:
                      $ref: '#/components/schemas/EventSummary'
                  - $ref: '#/components/schemas/Profile'

    post:
      tags: [LONG DESCRIPTION]
      summary: LONG DESCRIPTION - mark/unmark event
      description: normal. Adds or removes the event from the authenticated user's bookmarks.
      parameters:
        - in: header
          name: jwt
          required: true
          schema:
            type: string
        - in: path
          name: userid
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Participation'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Code'

  /vibeSearch:
    get:
      tags: [RECOMMENDATIONS]
      summary: RECOMMENDATIONS - vibe search
      description: |
        normal. Returns the top 5 upcoming events most semantically similar to the
        given prompt. If no prompt is provided, uses the authenticated user's saved
        preference string instead. Similarity is computed via vector search on a
        1024-dimensional `mxbai-embed-large` embedding stored on each event.

        Requires a MongoDB Atlas `cosine_index` on the `events.embedding` field.
      parameters:
        - in: header
          name: jwt
          required: true
          schema:
            type: string
        - in: query
          name: prompt
          required: false
          description: Free-text description of what you are looking for. Omit to use the user's saved preference.
          schema:
            type: string
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/EventSummary'
              example:
                - title: Science Fair
                  creator: teacher1
                  id: 664a1b2c3d4e5f6a7b8c9d0e

  /pic:
    post:
      tags: [PROFILE PICTURES]
      summary: PROFILE PICTURES - upload
      description: |
        Lives on the separate pictures service at port 5001, NOT on the main API.
        Upload a JPEG, PNG, or GIF (including animated) to be used as the
        authenticated user's profile picture. Target is always the user id
        carried by the JWT - users cannot upload on behalf of someone else.
        Max 5 MB. Replaces any previously uploaded picture.
      parameters:
        - in: header
          name: jwt
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                file:
                  type: string
                  format: binary
      responses:
        '200':
          description: OK
        '400':
          description: Missing file or unreadable image
        '413':
          description: File exceeds 5 MB
        '415':
          description: Unsupported format (only JPEG, PNG, GIF allowed)

  /pic/{userid}:
    get:
      tags: [PROFILE PICTURES]
      summary: PROFILE PICTURES - serve
      description: |
        Lives on the separate pictures service at port 5001, NOT on the main API.
        Returns the raw bytes of the user's profile picture with the original
        mime type (image/jpeg, image/png, or image/gif). GIFs are served untouched
        so animation is preserved. Returns 404 if the user has not uploaded one.
      parameters:
        - in: header
          name: jwt
          required: true
          schema:
            type: string
        - in: path
          name: userid
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Image bytes
          content:
            image/jpeg: {}
            image/png: {}
            image/gif: {}
        '404':
          description: No picture uploaded for this user

  /event:
    get:
      tags: [SEARCH PAGE]
      summary: SEARCH PAGE
      description: >-
        Full-text Atlas Search (`$search`) over event descriptions, ranked by relevance.
        The `title` param is the free-text query matched against the event description;
        `user` filters by publisher username. Only upcoming events are returned.
        Paginated with page_num (0-indexed, 10 per page). With no params, browses all
        upcoming events ordered by date ascending.
      parameters:
        - in: header
          name: jwt
          required: true
          schema:
            type: string
        - in: query
          name: title
          schema:
            type: string
        - in: query
          name: user
          schema:
            type: string
        - in: query
          name: page_num
          schema:
            type: integer
            default: 0
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/SearchResult'

    post:
      tags: [PROFILE]
      summary: PROFILE - create event
      description: Requires a verified account.
      parameters:
        - in: header
          name: jwt
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/NewEvent'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Code'

components:
  schemas:

    Code:
      type: object
      properties:
        code:
          type: integer

    Credentials:
      type: object
      properties:
        email:
          type: string
        password:
          type: string

    RegisterBody:
      type: object
      properties:
        email:
          type: string
        password:
          type: string
        username:
          type: string

    LoginResponse:
      type: object
      properties:
        code:
          type: integer
        jwt:
          type: string

    VerifyRequest:
      type: object
      properties:
        password:
          type: string

    EventSummary:
      type: object
      properties:
        title:
          type: string
        creator:
          type: string
        id:
          type: string

    EventDetail:
      type: object
      properties:
        title:
          type: string
        description:
          type: string
        date:
          type: string
        creator:
          type: string
        creatorid:
          type: string

    SearchResult:
      type: object
      properties:
        date:
          type: string
        title:
          type: string
        eventid:
          type: string
        creatorid:
          type: string
        creator:
          type: string

    Profile:
      type: object
      properties:
        email:
          type: string
        username:
          type: string
        bio:
          type: string
        profile_pic_url:
          type: string
          description: URL on the profile-pictures service where this user's picture can be fetched. Returns 404 if the user has not uploaded one.

    ProfilePatch:
      type: object
      properties:
        email:
          type: string
        username:
          type: string
        bio:
          type: string
        preference:
          type: string
          description: Free-text preference string used as the default vibe-search query when no prompt is supplied.

    Participation:
      type: object
      properties:
        eventid:
          type: string
        participating:
          type: boolean

    NewEvent:
      type: object
      properties:
        title:
          type: string
        description:
          type: string
        date:
          type: string
